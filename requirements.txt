# OCR Pipeline (YOLO + SVTR v6 + PaddleOCR + GUI)
# Python 3.8–3.11

# GUI
PySide6>=6.5.0

# Core DL/ML
torch>=1.9.0
torchvision>=0.10.0
ultralytics>=8.0.0

# OCR
paddlepaddle==2.5.1                 # GPU: dùng paddlepaddle-gpu>=2.5.1 (phụ thuộc CUDA)
paddleocr>=2.6.1.3
paddle-bfloat

# CV & xử lý ảnh
opencv-python>=4.6.0.66            # nếu gặp lỗi với numpy 2.x, thử hạ numpy<2.0 hoặc nâng opencv
numpy>=1.21
Pillow>=9.0.0
scikit-image>=0.19.0

# Tiện ích
PyYAML>=6.0
pathlib2>=2.3.0

# Tuỳ chọn (vẽ biểu đồ, debug)
matplotlib>=3.5.0
